{
  "routes": [{ "src": "/(.*)", "dest": "main.py" }],
  "build": {
    "env": {
      "PYTHON_VERSION": "3.9"  # 适配Vercel Python运行时，避免版本兼容问题
    },
    "requirements": "requirements.txt"  # 明确指定依赖文件，确保Vercel正确安装依赖
  }
}